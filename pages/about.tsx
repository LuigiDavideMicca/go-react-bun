export default function About() {
  return (
    <main>
      <h1>About</h1>
      <p>
        A mini full-stack framework experiment: file-based React pages with SSR,
        API routes written in Go, glued together by Bun. No webpack, no
        framework dependencies — just enough code to show how the pieces fit.
      </p>
      <a href="/">Back home</a>
    </main>
  );
}
