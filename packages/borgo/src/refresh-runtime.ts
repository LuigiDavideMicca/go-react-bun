// dev only: re-exported so generated app code resolves react-refresh
// through borgo, wherever the package manager placed it
// @ts-expect-error react-refresh ships no types
import RefreshRuntime from "react-refresh/runtime";

export default RefreshRuntime;
