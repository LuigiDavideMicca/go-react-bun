export default function About() {
  return (
    <main>
      <h1>About</h1>
      <p>
        This app is built with borgo: file-based React pages with SSR, API
        routes written in Go, glued together by Bun. The whole framework is
        small enough to read in one sitting.
      </p>
      <a href="/">Back home</a>
    </main>
  );
}
